<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jinhe.modules.system.dao.SysPermissionMapper">

    <select id="resourceIdByRoleId" resultType="com.jinhe.modules.system.dto.SysResourceDTO">
        SELECT
            r.*,
            (SELECT group_concat(resource_item_id) FROM sys_permission_item  pi WHERE pi.permission_id=p.id GROUP BY resource_item_id ) as resourceItemIds
        FROM
            sys_permission p
            LEFT JOIN sys_resource r ON r.id = p.resource_id
        WHERE
            p.role_id =#{roleId}
    </select>
</mapper>
